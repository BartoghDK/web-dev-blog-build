---
const pageTitle = 'About Me';

const identify = {
  firstName: 'Kim',
  middelName: 'John',
  lastName: 'Petersen',
  onlineName: 'BartoghDK',
  country: 'Denmark',
  occupation: 'Technical Writer',
  hobbies: ['photography', 'birdwatching', 'badminton'],
};

const skills = ['HTML', 'CSS', 'JavaScript', 'React', 'Astro', 'Writing Docs'];

// Definerer variablerne happy, finished and goal
const happy = true;
const finished = false;
const goal = 3;

const skillColor = 'navy';
const fontWeight = 'bold';
const textCase = 'uppercase';
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
    <!-- Here we style our about page -->
    <style define:vars="{{skillColor, fontWeight, textCase}}">
      h1 {
        color: purple;
        font-size: 4rem;
      }

      .skill {
        color: var(--skillColor);
        font-weight: var(--fontWeight);
        text-transform: var(--textCase);
      }
    </style>
  </head>
  <body>
    <a href="/">Home</a>
    <a href="/blog">Blog</a>
    <a href="/about">About Me</a>

    <h1>{pageTitle}</h1>
    <h2>... and my brand new learning Astro site!</h2>
    <p>
      I am currently working my way through Astro's intrtoductory tutorial on
      how to build websites with Astro. This is the second page on my website,
      and it's the first one I built myself, eventhough the words are not
      entirely my own!
    </p>
    <p>
      This site be updated as I complete more of the tutorial, so keep checking
      up on it, to see how my journey is going!
    </p>

    <h2>Here are a few facts about me:</h2>

    <ul>
      <li>
        My name is {identify.firstName}
        {identify.middelName}
        {identify.lastName}
      </li>
      <li>
        I live in {identify.country} and I work as a {identify.occupation}.
      </li>
      {
        identify.hobbies.length >= 2 && (
          <li>
            Two of my hobbies are {identify.hobbies[0]} and{' '}
            {identify.hobbies[2]}
          </li>
        )
      }
    </ul>

    <h2>My Skills Are</h2>
    <ul>
      {skills.map((skill) => <li class="skill">{skill}</li>)}
    </ul>

    <!-- Here will we give a usecase for the three defined variables of happy, finished and goal -->
    {happy && <p>I am happy to be learning Astro!</p>}
    {finished && <p>I finished the tutorial!</p>}
    {
      goal === 3 ? (
        <p>My goal is to finish in 3 days.</p>
      ) : (
        <p>My goal is not 3 days.</p>
      )
    }
  </body>
</html>
